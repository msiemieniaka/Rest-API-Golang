package config

const (
	DBHost     = "localhost"
	DBPort     = 5432
	DBUser     = "postgres"
	DBPassword = "root"
	DBName     = "marcinsiemieniaka" // Changed from postgres to a dedicated database
)
